/* Minimal loader for HTMX via CDN fallback.
   If you need offline, replace this file with a real htmx.min.js build. */
(function(){
  var s=document.createElement('script');
  s.src="https://unpkg.com/htmx.org@1.9.10";
  s.defer=true;
  document.head.appendChild(s);
})();
